<h1><%= @task.title %></h1>
<p><%= @task.details %></p>
<p><%= @task.completed %></p>

<%= link_to "Back to all tasks", tasks_path %>
<br>
<%= link_to "Destroy #{@task.title}", tasks_path(@task), method: :delete, data: { confirm: "Are you sure?"} %></br>
